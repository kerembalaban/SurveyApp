import { useNavigation } from '@react-navigation/native'
import React, { FC } from 'react'

const OldSurveys: FC = () => {
    const navigation = useNavigation()

    return (
        null
    )
}

export default OldSurveys